# Copy this file to .env (same folder) and fill in real values.
# .env is git-ignored and never leaves this machine.

# Required for the "Generate" button (Anthropic API — get a key at https://console.anthropic.com)
ANTHROPIC_API_KEY=
